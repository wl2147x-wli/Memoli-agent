# Optional environment variables for local development.
#
# The project should primarily read config.toml. Environment variables are
# reserved for secrets or local overrides that should not be committed.

MEMOLI_CONFIG=config.toml
MEMOLI_WORKSPACE=workspace
MEMOLI_LLM_API_KEY=
MEMOLI_LLM_BASE_URL=
