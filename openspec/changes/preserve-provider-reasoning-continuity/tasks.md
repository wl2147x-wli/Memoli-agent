## 1. 合同与配置

- [x] 1.1 在模型配置和请求合同中加入提供商中立的推理模式、推理强度与展示策略，并修复 `invoke_provider` 到 `ModelRequest` 的参数传递；通过配置解析单元测试验证显式启用、显式关闭和旧配置默认行为。
- [x] 1.2 建立协议与模型能力矩阵，在联网前拒绝无法表达的推理、工具、流式及展示策略组合；通过模拟传输断言无效组合不会产生任何网络请求。
- [x] 1.3 将可移植语义消息、允许展示的推理摘要和带协议标签的不透明续接信封拆分为不同合同；通过序列化测试验证通用消息与持久化载荷不包含签名、加密内容或响应续接标识。
- [x] 1.4 统一通用消息的规范化表示，使发送序列化、令牌估算、上下文哈希和缓存键基于相同语义内容；通过结构化块变更测试验证发送内容与哈希同步变化。

## 2. 提供商交换运行时

- [x] 2.1 实现仅存活于当前工具循环的 `ProviderExchange` 生命周期，并在最终回答、明确失败或取消时释放不透明状态；通过生命周期单元测试覆盖创建、续接、完成、失败和取消。
- [x] 2.2 在 Reasoner 工具循环和 `invoke_provider` 中传递交换标识、冻结的推理策略及不透明续接信封；通过回归测试验证启用推理的配置能够到达每一次实际模型请求。
- [x] 2.3 在交换首次成功调用后固定提供商、模型配置、模型和协议，仅允许在产生续接状态、部分流输出或工具副作用前降级；通过路由测试验证安全的前置降级成功、工具执行后的跨提供商降级被拒绝。
- [x] 2.4 让上下文编译器把活跃的助理工具调用和关联工具结果视为不可拆分后缀，并排除不透明续接状态；通过压缩边界测试验证后缀顺序和内容不变。
- [x] 2.5 对进程重启和新用户轮次采用明确的不可恢复语义，不根据可见文本重建续接状态；通过恢复测试验证系统返回稳定错误并使用已提交的可移植历史开始新交换。

## 3. 提供商适配器

- [x] 3.1 实现显式的 `openai-responses` 非流式适配路径，按类型和原始顺序解析推理项、消息项与函数调用项，并支持 `store=false` 的加密内容显式重放；通过协议固定样例验证标识、阶段、顺序和函数结果关联。
- [x] 3.2 实现 `openai-responses` 流式组装、取消、分类错误和用量归一化；通过模拟流测试验证多输出项、多个并行调用、未知必需项封闭式失败、取消关闭连接及推理令牌统计。
- [x] 3.3 更新 Anthropic 映射，在交换内冻结推理策略，并通过不透明信封完整、原序、未经修改地往返思考块、脱敏思考块、签名和工具调用块；通过省略展示、摘要展示、仅签名流和交错工具调用固定样例验证。
- [x] 3.4 保持现有 OpenAI Chat Completions 与已注册兼容方言的行为不变，并禁止根据模型名称或地址隐式切换到 Responses；通过现有适配器测试和新增协议选择测试验证向后兼容性。

## 4. 展示与数据安全

- [x] 4.1 增加 Agent 进度事件和提供商推理摘要事件的独立类型，并阻止原始推理、签名、加密内容、原始工具参数和 SDK 对象进入展示通道；通过事件载荷秘密扫描测试验证边界。
- [x] 4.2 保持 CLI 推理内容默认隐藏，并在显式选择 `summary` 或 `updates` 时进行有界、脱敏且标注“推理摘要”的渲染；通过 CLI 快照测试验证隐藏、摘要、更新、取消和错误场景。
- [x] 4.3 审计日志、Hook、SQLite 轨迹、JSONL 导出、检查点和长期记忆的写入边界；通过注入测试凭证、签名、加密推理和响应标识并扫描全部输出制品，验证不存在敏感值泄漏。

## 5. 集成验证与文档

- [x] 5.1 增加 OpenAI Responses 的端到端多工具循环测试，覆盖推理项、函数调用、工具结果和最终文本；通过流式与非流式结果的语义等价断言验证完整续接。
- [x] 5.2 增加 Anthropic 的端到端多工具循环测试，覆盖思考省略、推理摘要、脱敏块、策略中途变更和续接失败；通过线级请求断言验证所有必需块原样回传且失败时不切换提供商。
- [x] 5.3 增加共享适配器一致性测试，覆盖直接回答、工具参数组装、用量、重试、取消、分类错误和无凭证离线执行；通过默认测试套件不访问公共网络且不需要真实凭证进行验证。
- [x] 5.4 更新提供商、运行时、上下文管理和 CLI 文档及配置示例，说明 `openai-responses` 显式选择、三种展示策略、当前交换作用域和不保存原始思维链；通过文档链接检查和示例配置加载测试验证。
- [x] 5.5 运行 `python -m pytest -q`、`python -m ruff check memoli_agent benchmarks tests`、`python -m pyright` 和 `openspec validate preserve-provider-reasoning-continuity --strict`，并在四项命令均通过后完成本变更的实施验收。

## 6. Qwen vLLM 最终文本隔离

- [x] 6.1 新增显式 `qwen-vllm` 方言，将推理开关映射为 `chat_template_kwargs.enable_thinking` 的布尔值；通过请求固定样例验证关闭和启用均不依赖服务端默认值。
- [x] 6.2 在非流式和流式 Chat Completions 中排除 `reasoning_content`，并剥离完整或跨 chunk 的 `<think>...</think>` 块；通过协议固定样例验证仅最终回答进入响应和公共事件。
- [x] 6.3 更新本地配置示例与 vLLM 运维文档，说明 `qwen-vllm`、32768 上下文、硬关闭 thinking 和可选 reasoning parser；通过示例配置加载及文档链接检查验证。
- [x] 6.4 增加 Qwen vLLM 端到端回归，扫描 CLI、Hook、轨迹和聊天历史，验证思考内容与标签均不泄漏且工具调用保持可用。
- [x] 6.5 重新运行完整测试、Ruff、Pyright 和本变更 OpenSpec 严格校验，四项通过后完成补充实施验收。
